name: Deploy Frontend to Pages

on:
  push:
    branches: ["main"]
    paths:
      - "blackjack-game/frontend/**"
      - ".github/workflows/deploy-frontend.yml"
      - "blackjack-game/.github/workflows/deploy-frontend.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: blackjack-game/frontend/package-lock.json

      - name: Install dependencies
        working-directory: blackjack-game/frontend
        run: npm ci

      - name: Build frontend
        working-directory: blackjack-game/frontend
        env:
          VITE_API_BASE: ${{ vars.VITE_API_BASE }}
        run: npm run build -- --base=/${{ github.event.repository.name }}/

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: blackjack-game/frontend/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
